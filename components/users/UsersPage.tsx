"use client";
import { useState } from "react"; import type { Role, User } from "@/types";
export function UsersPage({users,currentUser,onChange}:{users:User[];currentUser:User;onChange:(u:User[])=>void}){const [form,setForm]=useState({username:"",password:"",fullName:"",role:"Dealer" as Role}); if(currentUser.role!=="Admin")return <p className="text-red-400">Access denied.</p>; function add(e:React.FormEvent){e.preventDefault();if(users.some(u=>u.username===form.username)){alert("Username already exists.");return}onChange([...users,{id:crypto.randomUUID(),...form,active:true}]);setForm({username:"",password:"",fullName:"",role:"Dealer"})} function toggle(id:string){onChange(users.map(u=>u.id===id?{...u,active:!u.active}:u))}
return <><div><p className="text-sm text-slate-500">Administration and access control</p><h2 className="mt-1 text-2xl font-semibold">User Management</h2></div><form onSubmit={add} className="mt-6 grid gap-4 rounded-2xl border border-slate-800 bg-[#0d1424] p-5 md:grid-cols-5"><input required placeholder="Full name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3"/><input required placeholder="Username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3"/><input required placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3"/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value as Role})} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3"><option>Admin</option><option>Manager</option><option>Dealer</option><option>Finance</option></select><button className="rounded-xl bg-blue-600 px-4 py-3">Create User</button></form><div className="mt-6 overflow-hidden rounded-2xl border border-slate-800 bg-[#0d1424]">{users.map(u=><div key={u.id} className="flex items-center justify-between border-b border-slate-800 p-5"><div><p className="font-medium">{u.fullName}</p><p className="mt-1 text-xs text-slate-500">{u.username} • {u.role}</p></div><button onClick={()=>toggle(u.id)} disabled={u.id===currentUser.id} className={`rounded-xl px-4 py-2 text-sm ${u.active?"bg-emerald-500/10 text-emerald-400":"bg-red-500/10 text-red-400"} disabled:opacity-40`}>{u.active?"Active":"Disabled"}</button></div>)}</div></>}
