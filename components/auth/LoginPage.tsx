"use client";
import { useState } from "react"; import type { User } from "@/types";
export function LoginPage({users,onLogin}:{users:User[];onLogin:(id:string)=>void}){
 const [username,setUsername]=useState("admin"),[password,setPassword]=useState("VTC2026"),[error,setError]=useState("");
 function submit(e:React.FormEvent){e.preventDefault(); const u=users.find(x=>x.active&&x.username.toLowerCase()===username.trim().toLowerCase()&&x.password===password); if(!u){setError("نام کاربری یا رمز عبور صحیح نیست.");return} setError("");onLogin(u.id)}
 return <main className="flex min-h-screen items-center justify-center bg-[#060a13] px-5 text-white"><div className="w-full max-w-md rounded-3xl border border-slate-800 bg-[#0d1424] p-8 shadow-2xl"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-blue-600 font-bold">V</div><p className="mt-8 text-xs uppercase tracking-[0.25em] text-blue-400">VTC ONE</p><h1 className="mt-2 text-3xl font-semibold">Secure Login</h1><p className="mt-2 text-sm text-slate-500">Enterprise Management System</p><form className="mt-8 space-y-5" onSubmit={submit}><label className="block"><span className="text-sm text-slate-400">Username</span><input value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-blue-500"/></label><label className="block"><span className="text-sm text-slate-400">Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-blue-500"/></label>{error&&<p className="text-sm text-red-400">{error}</p>}<button className="w-full rounded-xl bg-blue-600 py-3 font-semibold hover:bg-blue-500">Sign in</button></form><div className="mt-6 rounded-xl border border-slate-800 bg-slate-950/60 p-4 text-xs text-slate-500">Demo Admin: <b className="text-slate-300">admin</b> / <b className="text-slate-300">VTC2026</b></div></div></main>
}
