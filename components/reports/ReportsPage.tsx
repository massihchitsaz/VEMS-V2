import type { Deal, User } from "@/types";
export function ReportsPage({deals,users}:{deals:Deal[];users:User[]}){const completed=deals.filter(d=>d.status==="Completed").length,profit=deals.reduce((s,d)=>s+d.profit,0);const cards=[["Registered Deals",String(deals.length)],["Completed Deals",String(completed)],["Total Profit",`AED ${profit.toLocaleString()}`],["System Users",String(users.length)],["Active Dealers",String(users.filter(u=>u.active&&u.role==="Dealer").length)],["Pending Review",String(deals.filter(d=>d.status==="Review").length)]];return <><div><p className="text-sm text-slate-500">Management information</p><h2 className="mt-1 text-2xl font-semibold">Reports</h2></div><section className="mt-6 grid gap-4 md:grid-cols-3">{cards.map(([t,v])=><article key={t} className="rounded-2xl border border-slate-800 bg-[#0d1424] p-5"><p className="text-sm text-slate-500">{t}</p><p className="mt-5 text-2xl font-semibold">{v}</p></article>)}</section></>}
