"use client";
import { useMemo, useState } from "react";
import { useCommercialStore } from "@/hooks/useCommercialStore";
const money=new Intl.NumberFormat("en-US",{maximumFractionDigits:0});
export function QuotationsWorkbench(){const{quotations}=useCommercialStore();const[search,setSearch]=useState("");const rows=useMemo(()=>quotations.filter((q)=>`${q.reference} ${q.customer} ${q.subject} ${q.route}`.toLowerCase().includes(search.toLowerCase())),[quotations,search]);return <main className="p-5 text-white md:p-8"><header><p className="text-xs font-semibold uppercase tracking-[0.24em] text-blue-400">Pricing & Margin Control</p><h2 className="mt-2 text-3xl font-bold">Quotation Desk</h2><p className="mt-2 text-sm text-slate-400">Monitor pricing requests, validity, commercial margin and award status.</p></header><section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Open",quotations.filter(q=>!["awarded","expired"].includes(q.status)).length,"text-blue-400"],["Awaiting Rates",quotations.filter(q=>q.status==="awaiting-rates").length,"text-amber-300"],["Approval",quotations.filter(q=>q.status==="approval").length,"text-purple-400"],["Awarded",quotations.filter(q=>q.status==="awarded").length,"text-emerald-400"]].map(([l,v,c])=><article key={String(l)} className="rounded-2xl border border-slate-800 bg-[#0d1423] p-5"><p className="text-sm text-slate-400">{l}</p><p className={`mt-3 text-3xl font-bold ${c}`}>{v}</p></article>)}</section><input value={search} onChange={(e)=>setSearch(e.target.value)} placeholder="Search quotations..." className="mt-6 w-full rounded-xl border border-slate-800 bg-[#0d1423] px-4 py-3 outline-none focus:border-blue-500"/><section className="mt-5 overflow-hidden rounded-2xl border border-slate-800 bg-[#0d1423]"><div className="overflow-x-auto"><table className="w-full min-w-[1000px] text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase text-slate-500"><tr><th className="px-5 py-4">Reference</th><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Subject / Route</th><th className="px-5 py-4">Value</th><th className="px-5 py-4">Margin</th><th className="px-5 py-4">Valid Until</th><th className="px-5 py-4">Status</th></tr></thead><tbody>{rows.map(q=><tr key={q.id} className="border-b border-slate-800/80 last:border-0"><td className="px-5 py-4 font-semibold text-blue-300">{q.reference}</td><td className="px-5 py-4 font-semibold">{q.customer}</td><td className="px-5 py-4"><p>{q.subject}</p><p className="mt-1 text-xs text-slate-500">{q.route}</p></td><td className="px-5 py-4">{q.currency} {money.format(q.amount)}</td><td className="px-5 py-4 text-emerald-400">{q.marginPercent}%</td><td className="px-5 py-4">{q.validUntil}</td><td className="px-5 py-4 capitalize text-amber-300">{q.status.replaceAll("-"," ")}</td></tr>)}</tbody></table></div></section></main>}
